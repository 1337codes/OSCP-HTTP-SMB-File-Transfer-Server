# DualServe — tools.py requirements
# --------------------------------------------------------------------
# Script is ~entirely Python stdlib. Only external dep is impacket,
# used via the `impacket-smbserver` CLI subprocess (SMB mode only).
#
# HTTP-only mode → nothing in this file is required, just Python 3.
# SMB mode (-smb flag) → install impacket below.
#
# On Kali/Debian, `sudo apt install python3-impacket` is preferred —
# it puts the `impacket-smbserver` binary in PATH, which is what
# tools.py actually shells out to.
#
# pip install -r requirements.txt
# --------------------------------------------------------------------

# SMB server backend (optional — required for `tools.py -smb`)
impacket>=0.11.0

# Python 3.13+ shim: the stdlib `cgi` module was removed.
# Uncomment if you're on 3.13+ and the multipart upload handler chokes.
# legacy-cgi>=2.6.1
